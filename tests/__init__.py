"""Offline tests for agenttrace.

Everything here runs from the synthetic logs in ``tests/fixtures/``: no network, no
live capture and nothing outside this repository. Run them with::

    python3 -m unittest discover -s tests -t .
"""
